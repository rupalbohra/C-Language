// depending on different architectures, if I suppose that an integer is of n bytes in your architecture then
// the memory stored by 3 integers will be 3n 

// note: usually the size of an integer is 4 bytes but in old achitectures like turbo C it may differ.





// arrays are stored only in CONTINUOUS BLOCKS OF MEMORY. So, if I know address of any block I can calculate
// the address of any other block 